{
  "generation": 19,
  "population": [
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.099034\n    take_profit_pct = 0.25\n    trailing_stop_offset = 0.02\n    holding_period_days=21\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.050672\n    take_profit_pct=0.415220\n    trailing_stop_offset = 0.015\n    holding_period_days=24\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct = 0.10\n    take_profit_pct=0.318012\n    trailing_stop_offset = 0.02\n    holding_period_days = 30\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.073460\n    take_profit_pct=0.210671\n    trailing_stop_offset=0.013938\n    holding_period_days=22\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.114887\n    take_profit_pct=0.202473\n    trailing_stop_offset=0.019716\n    holding_period_days=39\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.093156\n    take_profit_pct=0.349941\n    trailing_stop_offset=0.018149\n    holding_period_days=17\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.063330\n    take_profit_pct = 0.25\n    trailing_stop_offset=0.017214\n    holding_period_days=26\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct = 0.08\n    take_profit_pct=0.294868\n    trailing_stop_offset=0.018782\n    holding_period_days = 20\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.149545\n    take_profit_pct = 0.25\n    trailing_stop_offset=0.018210\n    holding_period_days=31\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct = 0.08\n    take_profit_pct=0.318085\n    trailing_stop_offset = 0.015\n    holding_period_days=33\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.095948\n    take_profit_pct=0.238024\n    trailing_stop_offset=0.016924\n    holding_period_days=23\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.077545\n    take_profit_pct = 0.30\n    trailing_stop_offset = 0.015\n    holding_period_days=26\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.106069\n    take_profit_pct=0.205636\n    trailing_stop_offset=0.019471\n    holding_period_days=20\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.063915\n    take_profit_pct = 0.30\n    trailing_stop_offset=0.017387\n    holding_period_days=25\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.102286\n    take_profit_pct=0.230733\n    trailing_stop_offset=0.019660\n    holding_period_days = 30\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.091808\n    take_profit_pct = 0.30\n    trailing_stop_offset=0.014945\n    holding_period_days = 20\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.121845\n    take_profit_pct=0.262842\n    trailing_stop_offset = 0.02\n    holding_period_days = 30\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.063004\n    take_profit_pct = 0.30\n    trailing_stop_offset = 0.015\n    holding_period_days = 20\n\n    return signal",
    "def strategy(data):\n    # Turtle strategy with exit conditions\n    sma_fast = data['close'].rolling(20).mean()\n    sma_slow = data['close'].rolling(55).mean()\n\n    # Entry: Fast crosses above slow\n    signal = (sma_fast > sma_slow).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.125320\n    take_profit_pct=0.265976\n    trailing_stop_offset=0.024363\n    holding_period_days=33\n\n    return signal",
    "def strategy(data):\n    # Momentum strategy with exit conditions\n    returns = data['close'].pct_change(20)\n\n    # Entry: Top 20% momentum\n    signal = (returns.rank(pct=True) > 0.8).astype(int)\n\n    # Exit conditions (will be mutated)\n    stop_loss_pct=0.084267\n    take_profit_pct = 0.30\n    trailing_stop_offset=0.012033\n    holding_period_days = 20\n\n    return signal"
  ],
  "stats": {
    "generation": 19,
    "total_mutations": 20,
    "exit_mutations": 5,
    "exit_successes": 5,
    "exit_mutation_rate": 0.25,
    "exit_success_rate": 1.0,
    "bounds_violations": 0,
    "parameter_diversity": {
      "take_profit_pct": 0.08678263555340575,
      "trailing_stop_offset": 0.0,
      "stop_loss_pct": 0.0
    },
    "avg_diversity": 0.02892754518446858
  },
  "timestamp": "2025-10-28T11:33:54.510451"
}