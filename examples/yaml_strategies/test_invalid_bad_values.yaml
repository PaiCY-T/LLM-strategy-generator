# Invalid Strategy - Invalid Field Values
# ========================================
# This spec is INTENTIONALLY INVALID for testing validation
# Contains: invalid strategy_type, invalid indicator period, invalid stop_loss
# Expected: ValidationError

metadata:
  name: "Invalid Bad Values"
  description: "This strategy contains invalid field values"
  strategy_type: "trend_following"  # INVALID: not in enum (should be momentum/mean_reversion/factor_combination)
  rebalancing_frequency: "DAILY"    # INVALID: not in enum
  version: "1.0.0"

indicators:
  technical_indicators:
    - name: "rsi_invalid"
      type: "RSI"
      period: 500  # INVALID: exceeds maximum of 250

    - name: "InvalidName"  # INVALID: uppercase letters not allowed
      type: "SMA"
      period: 50

entry_conditions:
  threshold_rules:
    - condition: "rsi_invalid > 30"

exit_conditions:
  stop_loss_pct: 0.85  # INVALID: exceeds maximum of 0.50
  take_profit_pct: -0.10  # INVALID: negative value not allowed
