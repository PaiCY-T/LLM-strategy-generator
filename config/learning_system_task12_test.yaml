# Temporary Test Configuration for Task 12
# This file is used only for testing LLM integration

llm:
  enabled: true
  provider: gemini
  model: gemini-2.0-flash
  innovation_rate: 0.20
  timeout: 60
  max_tokens: 2000
  temperature: 0.7

sandbox:
  enabled: false

anti_churn:
  probation_period: 2
  probation_threshold: 0.10
  post_probation_threshold: 0.05

features:
  enable_anti_churn: true

multi_objective:
  enabled: false
